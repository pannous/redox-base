[workspace]

[package]
name = "curl"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "curl"
path = "src/main.rs"

[dependencies]
# Use ureq for HTTP with TLS support
ureq = { version = "2", features = ["tls"] }

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
