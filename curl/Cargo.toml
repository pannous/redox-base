[workspace]

[package]
name = "curl"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "curl"
path = "src/main.rs"

[dependencies]
# Use ureq for HTTP with default features (includes std networking)
ureq = "2"

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
